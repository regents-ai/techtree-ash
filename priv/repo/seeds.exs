# There is nothing to seed.
#
# Every row this application serves comes from importing a generated catalog
# bundle, which is an explicit release command rather than part of setup:
#
#     mix catalog.import --path priv/catalog
#
# Inventing catalog rows here would create public objects that no digest
# addresses and that `techtree-python` never generated.
